apiVersion: apps/v1
kind: Deployment
metadata:
  name: payments-mongo-depl
  namespace: default 
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payments-mongo
  template:
    metadata:
      labels:
        app: payments-mongo
    spec:
      containers:
        - name: payments-mongo
          image: mongo:4.4
          #imagePullPolicy: Never
---
apiVersion: v1
kind: Service
metadata:
  name: payments-mongo-srv
  namespace: default
spec:
  selector:
    app: payments-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
